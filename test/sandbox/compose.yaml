services:
  sandbox:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        SOURCEMETA_REGISTRY_EDITION: ${SOURCEMETA_REGISTRY_EDITION}
        SOURCEMETA_REGISTRY_CONFIGURATION: ${SOURCEMETA_REGISTRY_CONFIGURATION}
    ports:
      - 8000:8000
